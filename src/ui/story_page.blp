using Gtk 4.0;
using Adw 1;

template $StoryPage: Adw.NavigationPage {
    Adw.ToolbarView {
        top-bar-style: flat;

        [top]
        Adw.HeaderBar {
            decoration-layout: "";
            show-back-button: true;
        }
    }
}
